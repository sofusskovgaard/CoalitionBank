apiVersion: apps/v1
kind: Deployment
metadata:
  name: accounts-service
spec:
  selector:
    matchLabels:
      app: accounts-service
  template:
    metadata:
      labels:
        app: accounts-service
    spec:
      containers:
        - image: coalitionbank.azurecr.io/accounts-service:latest
          name: accounts-service
          resources:
            requests:
              cpu: 200m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
          ports:
            - containerPort: 80
              name: http